<UserControl x:Class="EZSampler.UI.Wpf.Controls.WaveformView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:ui="http://schemas.modernwpf.com/2019"
             mc:Ignorable="d">

    <Border BorderThickness="1" 
            BorderBrush="#D5D4D2" 
            Background="#F5F5F3"
            CornerRadius="4">
        <Grid>
            <Canvas x:Name="WaveformCanvas"
                    MouseLeftButtonDown="WaveformCanvas_MouseLeftButtonDown"
                    MouseMove="WaveformCanvas_MouseMove"
                    MouseLeftButtonUp="WaveformCanvas_MouseLeftButtonUp">
                <Path x:Name="WaveformPath" 
                      Stroke="#2E2E2E" 
                      StrokeThickness="1.5" />
            </Canvas>
            <Line x:Name="PlaybackPositionLine"
                  Visibility="Collapsed"
                  Stroke="#E53935"
                  StrokeThickness="2"
                  X1="0"
                  X2="0"
                  Y1="0"
                  Y2="200"
                  RenderTransformOrigin="0.5,0.5"
                  IsHitTestVisible="False">
                <Line.RenderTransform>
                    <TranslateTransform x:Name="PlaybackPositionTransform" X="0" />
                </Line.RenderTransform>
            </Line>
            <StackPanel Orientation="Horizontal"
                        HorizontalAlignment="Right"
                        VerticalAlignment="Top"
                        Margin="12">
                <Button x:Name="ClearButton"
                        Width="90"
                        Height="32"
                        Margin="0,0,8,0"
                        IsEnabled="False"
                        Click="ClearButton_Click">
                    Clear
                </Button>
                <Button x:Name="SaveButton"
                        Width="90"
                        Height="32"
                        IsEnabled="False"
                        Click="SaveButton_Click"
                        Style="{StaticResource AccentButtonStyle}">
                    💾 Save
                </Button>
            </StackPanel>
        </Grid>
    </Border>
</UserControl>
